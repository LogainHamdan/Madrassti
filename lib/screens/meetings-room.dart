import 'package:flutter/material.dart';

class MeetingsRoomScreen extends StatelessWidget {
  const MeetingsRoomScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
